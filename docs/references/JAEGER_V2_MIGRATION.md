# Jaeger v1 to v2 Migration Summary

## Migration Status: ✅ COMPLETED

### What Changed

**Jaeger v1 (all-in-one)** → **Jaeger v2 (OpenTelemetry-based)**

- Image: `jaegertracing/all-in-one:latest` → `jaegertracing/jaeger:2.2.0`
- Version: v1.74.0 → v2.2.0
- Architecture: Monolithic → OpenTelemetry Collector-based
- Configuration: Environment variables → YAML config file

### Current Configuration

#### Docker Compose
```yaml
jaeger:
  image: jaegertracing/jaeger:2.2.0
  command: ["--config", "/etc/jaeger/config.yaml"]
  volumes:
    - ./config/jaeger/jaeger-config.yaml:/etc/jaeger/config.yaml:ro
  ports:
    - "16686:16686"  # Jaeger UI
    - "16685:16685"  # Jaeger gRPC Query
  environment:
    - METRICS_STORAGE_TYPE=prometheus
    - PROMETHEUS_SERVER_URL=http://prometheus:9090
    - PROMETHEUS_QUERY_SUPPORT_SPANMETRICS_CONNECTOR=true
    - PROMETHEUS_QUERY_NAMESPACE=telemetryflow_core
    - PROMETHEUS_QUERY_DURATION_UNIT=ms
    - PROMETHEUS_QUERY_NORMALIZE_CALLS=true
    - PROMETHEUS_QUERY_NORMALIZE_DURATION=true
```

#### Jaeger Config File (`config/jaeger/jaeger-config.yaml`)
```yaml
extensions:
  jaeger_storage:
    backends:
      some_storage:
        memory:
          max_traces: 100000
  
  jaeger_query:
    storage:
      traces: some_storage

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318
  
  zipkin:
    endpoint: 0.0.0.0:9411

processors:
  batch:

exporters:
  jaeger_storage_exporter:
    trace_storage: some_storage

service:
  extensions: [jaeger_storage, jaeger_query]
  
  pipelines:
    traces:
      receivers: [otlp, zipkin]
      processors: [batch]
      exporters: [jaeger_storage_exporter]
```

### SPM (Service Performance Monitoring) Status

**Issue**: Metrics storage not configured in Jaeger v2

**Root Cause**: Jaeger v2.2.0 doesn't support Prometheus metrics storage via environment variables when using custom config file.

**Workaround Options**:

1. **Use Jaeger v2 without custom config** (environment variables only)
2. **Wait for Jaeger v2.3+** with better Prometheus integration
3. **Query Prometheus directly** for span metrics

### Current Span Metrics Available

Metrics are being generated by OTEL Collector spanmetrics connector:

```
telemetryflow_core_span_metrics_calls_total
telemetryflow_core_span_metrics_duration_milliseconds_bucket
telemetryflow_core_span_metrics_duration_milliseconds_count
telemetryflow_core_span_metrics_duration_milliseconds_sum
```

**Query in Prometheus**:
```promql
# Call rate
rate(telemetryflow_core_span_metrics_calls_total[5m])

# Average duration
rate(telemetryflow_core_span_metrics_duration_milliseconds_sum[5m]) 
/ 
rate(telemetryflow_core_span_metrics_duration_milliseconds_count[5m])

# P95 latency
histogram_quantile(0.95, 
  rate(telemetryflow_core_span_metrics_duration_milliseconds_bucket[5m])
)
```

### OTEL Collector Configuration

Updated `config/otel/otel-collector-config.yaml` with spanmetrics connector:

```yaml
connectors:
  spanmetrics:
    namespace: span.metrics
    dimensions:
      - name: http.method
      - name: http.status_code
      - name: service.name
    histogram:
      explicit:
        buckets: [0.002, 0.004, 0.008, 0.016, 0.032, 0.064, 0.128, 0.256, 0.512, 1.024, 2.048, 4.096, 8.192, 15]
    metrics_flush_interval: 15s

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [memory_limiter, resourcedetection, batch, attributes]
      exporters: [otlp/jaeger, spanmetrics, debug]

    metrics:
      receivers: [otlp, prometheus, spanmetrics]
      processors: [memory_limiter, batch]
      exporters: [prometheus, debug]
```

### Testing

1. **Check Jaeger UI**: http://localhost:16686
2. **Check Prometheus**: http://localhost:9090
3. **Query span metrics**:
   ```bash
   curl 'http://localhost:9090/api/v1/query?query=telemetryflow_core_span_metrics_calls_total'
   ```

### Next Steps

1. Monitor Jaeger v2 releases for improved Prometheus integration
2. Consider using Grafana dashboards to visualize span metrics from Prometheus
3. Evaluate alternative: Keep Jaeger v1 for SPM until v2 support matures

### References

- [Jaeger v2 Migration Guide](https://www.jaegertracing.io/docs/latest/migration/)
- [Jaeger v2 Configuration](https://www.jaegertracing.io/docs/latest/deployment/)
- [OpenTelemetry Span Metrics Connector](https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/connector/spanmetricsconnector)
